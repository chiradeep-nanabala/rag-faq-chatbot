[project]
name = "rag-faq-chatbot"
version = "0.1.0"
description = "A simple RAG chatbot for FAQ documents using OpenAI embeddings and GPT"
requires-python = ">=3.11"
dependencies = [
    "openai>=1.30.0",
    "numpy>=1.26.0",
]
